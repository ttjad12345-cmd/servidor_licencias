const express = require('express'); 
const app = express(); 
app.use(express.json()); 
const licenciasNube = { "PRO-2026-JUAN": { plan: "pro", areas: 6, activa: true, hardwareId: null }, "START-2026-ANA": { plan: "starter", areas: 2, activa: true, hardwareId: null } }; 
app.listen(PORT, () = activo en puerto ${PORT}`)); 
